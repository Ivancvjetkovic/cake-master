import 'package:flutter/material.dart';

class Screen_menu extends StatelessWidget {
  const Screen_menu({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
